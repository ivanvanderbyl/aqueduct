{
  "name": "aqueduct",
  "version": "1.0.6",
  "description": "Aqueduct: API and Socket client for ERC dEXCHANGE",
  "main": "./dist/generated/aqueduct.js",
  "typings": "./dist/generated/aqueduct.d.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/ERCdEX/aqueduct.git"
  },
  "author": "ERC dEx",
  "license": "MIT",
  "scripts": {
    "prepublish": "npm run build:lib",
    "deploy": "npm version patch -m \"Release v%s\" && npm publish",
    "postversion": "git push origin master && git push --follow-tags",
    "build": "tsc",
    "start": "tsc -w",
    "build:lib": "rimraf dist && npm run generate:client && npm run build",
    "build:lib:fresh": "npm run fetch:swagger && npm run fetch:events && npm run build:lib",
    "start:docs": "npm run generate:docs && cd docs && npm start",
    "build:docs": "npm run generate:docs && cd docs && npm run build:prod",
    "generate:client": "ts-node ./generate-client.ts --baseApiUrl=http://localhost:8080",
    "generate:docs": "npm run build:lib && rimraf ./docs/src/frames/typedoc && typedoc --excludeExternals --excludePrivate --theme themes/custom --readme none --name \"Aqueduct JavaScript Client\" --mode modules --out ./docs/src/frames/typedoc ./src/generated/aqueduct.ts",
    "fetch:swagger": "ts-node ./fetch-swagger --baseApiUrl=http://localhost:8080",
    "fetch:events": "ts-node ./events/fetch-events --baseApiUrl=http://localhost:8080"
  },
  "dependencies": {
    "moment": "^2.19.1",
    "socket.io-client": "^2.0.3",
    "superagent": "^3.7.0"
  },
  "devDependencies": {
    "@types/handlebars": "^4.0.36",
    "@types/socket.io-client": "^1.4.31",
    "@types/superagent": "^3.5.6",
    "@types/yargs": "^8.0.2",
    "json-schema-to-typescript": "^4.6.5",
    "handlebars": "^4.0.11",
    "rimraf": "^2.6.2",
    "ts-node": "^3.3.0",
    "tslint": "^5.8.0",
    "typedoc": "^0.9.0",
    "typescript": "^2.5.3",
    "yargs": "^9.0.1"
  }
}
